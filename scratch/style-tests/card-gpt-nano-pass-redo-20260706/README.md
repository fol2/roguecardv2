# GPT -> Nano Pass Redo Review - 2026-07-06

User-requested redo set for:

- `warCry`
- `oblivionStrike`
- `quakeblow`
- `ashenChoir`
- `nightSight`

All five cards were regenerated with Nano Banana Pro image-to-image using the
same dedicated GPT -> Nano Pass prompt: short Nano Banana pass prompt plus style
frame only. No stored GPT prompt, identity check, card frame, border, local
brightness/contrast edit, crop, resize, or other post-processing was used for
the accepted gallery assets.

Review sheet:

```text
warCry-oblivionStrike-quakeblow-ashenChoir-nightSight-redo-contact.jpg
```

Rows show:

1. live GPT inputs;
2. superseded GPT -> Nano pass outputs;
3. accepted redo outputs.

Accepted redo outputs are copied into their original batch source folders as
`source/<id>-gpt-nano-pass.jpg` and into
`src/assets-gpt-nano-pass/cards/<id>.jpg`. Superseded originals are preserved in
their batch source folders as `source/<id>-gpt-nano-pass-v1-superseded-by-redo.jpg`.
