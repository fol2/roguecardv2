# Card First Three Scene Readable Baseline

Generated on 2026-07-06 with built-in Codex Image Gen.

This replaces the rejected badge-style first pass. The user correction was that
card art needs a rectangular background and a foreground subject/action, not a
floating emblem.

Parked in the inactive gallery set:

- `src/assets-readable-baseline/cards/strike.png`
- `src/assets-readable-baseline/cards/defend.png`
- `src/assets-readable-baseline/cards/empower.png`

## Files

- `source/strike-scene-source.png` - built-in Image Gen source.
- `source/defend-scene-source.png` - built-in Image Gen source.
- `source/empower-scene-source.png` - built-in Image Gen source.
- `rejected-badge-v1/` - previous badge-style baseline copied before
  replacement.
- `readability-contact.png` - dark-background review at large and small
  card-art-band sizes.

## Processing

1. Generated full-bleed rectangular scene art with built-in Image Gen.
2. Did not remove the background; the rectangle is part of the card art.
3. Resized and centre-cropped each source to `800 x 500`, matching the broad
   `.card-art` band better than the earlier square badge assets.

## Read

- `strike` now reads as a blade action in front of a red chapel window.
- `defend` now reads as a ward scene with lantern, body, impact, and blue glass.
- `empower` now reads as a persistent power oath, not a heart-flame badge.
