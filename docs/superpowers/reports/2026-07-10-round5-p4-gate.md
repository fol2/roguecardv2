# Round 5 P4 → P5 gate

**Date:** 2026-07-14 (UTC)  
**Host tip:** `864f0e4bd40caef4f2271efaf14ff3b3ba05d60f`  
**Branch:** `jamesto/round5-production-engineering-continuation`  
**Machine:** Darwin arm64 (local gate; no GitHub standing / full CI wait)

## Decision: P4 PREFIX EXIT

Non-visual P4 gates do **not** all pass at tip `864f0e4b`. Owner cadence preferred `GO TO P5` only when every non-visual P4 gate is green (with deferred Task 24 Step 6 baselines documented as ship follow-up). Hard blockers below prevent that path. This record does **not** open the prefix ship PR, regenerate `dist/**`, or run standing — parent owns broad-review / prefix closure if still desired.

This gate makes **no** actual-Safari, iOS/iPadOS Simulator, WKWebView, hardware, packaging, or mobile-support claim. WebKit evidence is Playwright’s patched WebKit with phone/pad device descriptors only.

---

## Tip SHAs (Tasks 21–24)

| Task | Tip SHA | Notes |
|---|---|---|
| 21 Pixi bootstrap | `5d388daeb7cee05bbecffc8ecaddc558e373c42d` | After `cbfe90c4` bootstrap + `d0ad14e4` Spec harden; last before Task 22a |
| 22 Combat chrome cutover | `1ee44967c99c65c0be8b6ae0e9ad7f584582e8bb` | `feat: close Task 22 combat chrome cutover (bfuieditor-disk + inventory)` |
| 23 Input router / Probe v2 | `c9d95b4ada946266d86f8b1d547328c83a38cacf` | After `0809409c` centralise; mouse play + `aimMove` harden |
| 24 Recovery / profile / perf / policy | `864f0e4bd40caef4f2271efaf14ff3b3ba05d60f` | Source head after `05788868` + `26d2af3c` + contrast/bundle harden; **Step 6 baselines not landed** |

---

## Task 24 Step 6 baseline status

**Deferred / not executed at this tip.**

No Darwin `test:e2e:update`, no `test:e2e:visual` recapture, and no Linux `update-baselines.yml` → `install-baseline-artifact.mjs` cycle was run for the P4 Pixi source head. Frozen policy source remains `VISUAL_DIFF_RATIOS` all `0.01` (`test/e2e/visual-policy.js`); Linux baseline tooling (`write-baseline-manifest` / `run-baseline-workflow` / `install-baseline-artifact`) is in tree via Task 24 Steps 1–5, but **in-phase dual-platform baseline review is outstanding**.

Any later `GO TO P5` or prefix ship still requires Task 24 Step 6–7 baseline recapture + FE/spec/code-quality review before treating visual contract as closed.

Visual suite was **not** used as a GO/NO-GO input here (owner cadence: prefer non-visual + focused desktop lanes when baselines are stale).

---

## Step 1 command evidence (actually run)

Working tree at start: clean at `864f0e4b`. Commands run under
`/Users/jamesto/Coding/roguecardv2/.worktrees/round5-production-engineering-continuation`.
Logs: `/tmp/round5-p4-gate/`.

### Node / budget / surface / hygiene

| Command | Exit | Summary |
|---|---|---|
| `npm run test:ci` | **0** | ci contract + module boundaries + baseline tool checks passed |
| `npm test` | **0** | `unit checks passed; monte-carlo: 300 runs, 1 random-agent wins, 299 deaths` |
| `npm run test:progression` | **0** | guided median=18 (2000/2000); unguided median=50 (1991/2000) |
| `npm run test:act-coupling` | **0** | `14 findings, all allowlisted (14 rows)` |
| `node tools/check-bundle-budget.mjs` | **0** | `entry … gzip bytes (max 451584, pre-Pixi baseline 323218)` — under budget |
| `node tools/verify-production-surface.mjs` | **1** | `production surface leaked DEV markers: data-dev-shell in …/assets/index-….js` |
| `git diff --check` | **0** | clean |

**Production-surface root cause (blocking):** production chunk still contains the literal `data-dev-shell` from `src/ui/pointer.js` (`closest(…, [data-dev-shell])`). DEV shell mounts only under `import.meta.env.DEV`; the string still fails the frozen marker scan.

### Focused desktop Playwright (serial, `--workers=1 --no-deps`)

```text
node tools/run-with-strict-e2e-port.mjs -- npx playwright test \
  pixi input-router battle geometry production-profile trace \
  --project=desktop --workers=1 --no-deps
```

| Result | Count |
|---|---|
| passed | 74 |
| failed | **4** |
| skipped | 5 |
| wall | ~7.9m |
| process exit | **1** |

**Passed (desktop):** all `pixi` (incl. recovery text projection / freeze / WebGL owners, five-shape candle frames, contrast), `input-router` (Probe v2 shape, no hit proxies, 26px drag / 12px cancel, real pointer defend), `battle`, `geometry`, Chromium `production-profile` fresh + veteran.

**Failed (desktop `trace.spec.js`):**

1. `frozen Task 6 fixture manifest…` — `renderer.kind` race and/or `generation >= 1` against combat-gl `generation` (starts at `0` until rebuild; Pixi layer generation is separate). Reproduced in isolation (`REAL_EXIT:1`).
2. `persistence fixtures: initial run, Usurper and Shade…` — click timeout under full suite (isolated retry **passed**).
3. `persistence fixtures: terminal outbox…` — `[data-a="retry-end"]` not focused / missing.
4. `persistence fixtures: Dawn cursor and final clear…` — `waitForDawnComplete` timeout.

### `test:e2e:nonvisual` (partitioned substitute for full `test:e2e`)

```text
node tools/run-with-strict-e2e-port.mjs -- npm run test:e2e:nonvisual
```

| Result | Count |
|---|---|
| passed | 316 |
| failed | **34** |
| skipped | 241 |
| wall | ~19.7m |
| exit | **1** |

Failures clustered on Lab journeys, Emberglass persistence/dawn, the same Task 6 fixture assertion, some persistence fixtures, plus scattered audio / bfeditor / geometry / input-router timeouts. **Note:** this lane overlapped host load with perf + WebKit runs; treat as supporting evidence, not the sole serial verdict. Disk editors did pass early: `bfuieditor` Save → `ui-chrome-layout.js` live Pixi bounds (**exit 0** for that case).

### Perf (PERF_WARNING allowed)

| Command | Exit | Metrics |
|---|---|---|
| `npm run test:e2e:perf` (portrait / LITE) | **0** | `PERF_RESULT {"avgFps":68.97,"p95FrameMs":14.4,"worstFrameMs":80.1,"frames":207,"project":"portrait","tier":"lite","pixiReady":true}` — meets ≥55 fps / ≤22 ms |
| `PERF=1 PERF_TIER=full … perf --project=desktop` | **0** | `PERF_RESULT {"avgFps":49.3,"p95FrameMs":26.8,"worstFrameMs":613.6,"frames":148,"project":"desktop","tier":"full","pixiReady":true}` + **`PERF_WARNING`** average 49.3 &lt; 55; p95 26.8 &gt; 22 — **valid target miss, non-blocking** |

Measurements present, finite, Pixi ready asserted — not missing/crashed.

### WebKit production-profile (time permitted)

```text
node tools/run-with-strict-e2e-port.mjs -- npx playwright test production-profile \
  --project=iphone-webkit --project=ipad-webkit --workers=1 --no-deps
```

| Result | Count |
|---|---|
| failed | **4 / 4** |
| exit | **1** |

All fresh/veteran × iphone/ipad journeys failed `expectNoErrors` on:

`console.error: WebGL: INVALID_OPERATION: loseContext: context already lost`

Chromium desktop production-profile in the focused lane was green; WebKit device-emulation lane is not.

---

## Non-command evidence

| Area | Evidence |
|---|---|
| Trace parity | Frozen Task 6 fixture **assertion does not hold** at tip (kind/generation wiring + wait race). Other desktop trace cases in the focused run largely passed (screen/cancel/play order, drain recovery, Music Cue warm graph, draw-wave failure owners, etc.). |
| Pointer / cancel | Focused `input-router`: 26px upward starts drag; 12px cancels long-press; cancel restores seat; real pointer defend through stage router — **pass**. |
| Context recovery | Focused `pixi` P4 recovery text projection (lost→rebuilding→ready), freeze identity, three named WebGL owners — **pass**. |
| Shake | Covered by Pixi shake-sync / recovery suite cases in focused `pixi` — **pass**. |
| Production-profile | Chromium desktop fresh+veteran — **pass**. Playwright WebKit iphone+ipad fresh+veteran — **fail** (`loseContext` already lost). |
| bfuiedit | Disk Save writes `ui-chrome-layout.js`; live Pixi keeps new bounds — **pass**. |
| Stage shapes | Focused `pixi` “five stage shapes map to authored candle frames” — **pass**. |
| PERF | Portrait/LITE green; desktop/Full **PERF_WARNING** recorded (allowed). |
| Safari / Simulator / hardware | **Explicitly none claimed.** |

---

## Blockers (must clear before GO TO P5)

1. **`verify-production-surface.mjs` exit 1** — remove or rewrite the production `data-dev-shell` string leak from `src/ui/pointer.js` (or otherwise keep the marker out of release chunks) and re-run the verifier.
2. **Task 6 fixture / Probe generation contract** — fix wait (do not proceed on bare `__probe`) and assert the correct generation field (`pixi` / `pixiGeneration`, not combat-gl `generation` which stays `0` until rebuild); re-run desktop `trace` fixture test in isolation + suite.
3. **WebKit production-profile** — stop emitting `loseContext: context already lost` into `expectNoErrors` (guard double-lose, or allowlist that console noise only if product path is proven correct); re-run iphone+ipad fresh/veteran.
4. **Task 24 Step 6–7** — Darwin update + visual verify + Linux manifest install + dual-platform review before ship treats the Pixi visual contract as closed.
5. **Re-run** focused desktop matrix + a clean (non-contended) `test:e2e:nonvisual` after the above; investigate remaining Lab/Emberglass failures if they persist without parallel load.

---

## What already looks P4-ready (non-blocking alone)

- CI contract / unit / progression / act-coupling / bundle gzip budget / whitespace check
- Desktop Pixi chrome, input router, battle, geometry, Chromium production-profile
- Portrait/LITE perf in budget; Full-tier miss correctly warned
- bfuiedit disk path; five-shape candle mapping
- Visual baselines deferred with policy frozen at `0.01` (follow-up, not a substitute for blockers 1–3)

---

## Cadence notes

- No `npm run test:round5:standing -- --profile p4`
- No full GitHub CI / required-check wait
- No `test:e2e:visual` / `test:e2e:update` in this gate (baselines deferred)
- Prefix ship (`build` + `dist/**` + PR) **not** executed — Decision is PREFIX EXIT for protocol honesty; owner may still choose fix-forward to a later GO tip instead of shipping the P4 prefix
