# Round 5 P4 → P5 gate

**Date:** 2026-07-14 (UTC)  
**Host tip (Decision):** `6d183400aff4178bdbac33cb767d69f9d4414bd2`
**Branch:** `jamesto/round5-production-engineering-continuation`  
**Machine:** Darwin arm64 (local gate; no GitHub standing / full CI wait)

## Decision: GO TO P5

Owner continues Tasks 26–40. Hard non-visual P4 blockers that forced the earlier **PREFIX EXIT** at `312bfeb0` / tip `864f0e4b` are cleared at this tip. **Task 24 Step 6 dual-platform baseline recapture remains deferred** and does **not** alone force PREFIX EXIT — it is a commercial-ship / visual-contract follow-up before treating screenshots as closed.

This record does **not** open a prefix ship PR, regenerate `dist/**`, or run standing.

This gate makes **no** actual-Safari, iOS/iPadOS Simulator, WKWebView, hardware, packaging, or mobile-support claim. WebKit evidence is Playwright’s patched WebKit with phone/pad device descriptors only.

---

## Tip SHAs (Tasks 21–24 + GO fix)

| Task | Tip SHA | Notes |
|---|---|---|
| 21 Pixi bootstrap | `5d388daeb7cee05bbecffc8ecaddc558e373c42d` | After `cbfe90c4` bootstrap + `d0ad14e4` Spec harden; last before Task 22a |
| 22 Combat chrome cutover | `1ee44967c99c65c0be8b6ae0e9ad7f584582e8bb` | `feat: close Task 22 combat chrome cutover (bfuieditor-disk + inventory)` |
| 23 Input router / Probe v2 | `c9d95b4ada946266d86f8b1d547328c83a38cacf` | After `0809409c` centralise; mouse play + `aimMove` harden |
| 24 Recovery / profile / perf / policy | `864f0e4bd40caef4f2271efaf14ff3b3ba05d60f` | Source head after `05788868` + `26d2af3c` + contrast/bundle harden; **Step 6 baselines not landed** |
| GO TO P5 blocker clear | `5ba24afcfefceba0a3edccf0b569c3fd3b236246` | `data-dev-shell` DEV-gate; Task 6 pixi generation assert; WebKit-safe loseContext teardown |

---

## Blockers cleared (PREFIX EXIT → GO)

### 1. `verify-production-surface` — `data-dev-shell` leak

**Was:** production chunk contained literal `data-dev-shell` from `src/ui/pointer.js` (`closest(…, [data-dev-shell])`).

**Fix:** split overlay vs DEV-shell checks; DEV-shell selector lives only under `import.meta.env.DEV` so release DCE drops the marker.

**Verify:** `node tools/verify-production-surface.mjs` → **0** (`production surface clean (12 markers checked)`).

### 2. Trace Task 6 kind / generation

**Was:** wait proceeded on bare `__probe`; assert used combat-gl `generation` (stays `0` until remount/rebuild).

**Fix:** wait for `spirebound.pixi.status() === 'ready'`; assert `kind === 'pixi'` and **`pixiGeneration >= 1`** separately (comparison-only fixtures untouched).

**Verify:** desktop `trace.spec.js` “frozen Task 6 fixture…” → **pass** (isolated).

### 3. WebKit production-profile `loseContext: context already lost`

**Was:** after test loss, Pixi `GlContextSystem.destroy()` called `loseContext()` again; WebKit logged `INVALID_OPERATION` as `console.error` → `expectNoErrors` fail.

**Fix:** `loseContextForTest` idempotent if already lost; before `application.destroy` when lost, null Pixi’s stored `context.extensions.loseContext` so destroy does not double-lose.

**Verify:** `production-profile` × `iphone-webkit` + `ipad-webkit` fresh+veteran → **4/4 pass**. Desktop recovery / production-profile recheck → **pass**.

### 4. Task 24 Step 6 baselines (deferred — not a GO blocker)

**Still deferred / not executed at this tip.**

No Darwin `test:e2e:update`, no `test:e2e:visual` recapture, and no Linux `update-baselines.yml` → `install-baseline-artifact.mjs` cycle for the P4 Pixi source head. Frozen policy source remains `VISUAL_DIFF_RATIOS` all `0.01` (`test/e2e/visual-policy.js`). Dual-platform baseline review remains a **ship follow-up** before the visual contract is closed; owner continues P5 with this note.

### 5. `test:e2e:nonvisual` mass failures (contention note)

Prior PREFIX EXIT run: 316 passed / **34 failed** under host load overlapping perf + WebKit. Failures clustered on Lab / Emberglass persistence-dawn, the Task 6 assert (now fixed), and scattered timeouts. **Not re-boiled** for this GO tip — only clear P4 regressions above were fixed. Treat that mass lane as supporting evidence under contention, not a serial veto when focused non-visual P4 gates are green.

---

## Step 1 command evidence (GO tip)

Working tree under
`/Users/jamesto/Coding/roguecardv2/.worktrees/round5-production-engineering-continuation`.
Logs: `/tmp/round5-p4-gate/*-fix*.log`.

### Node / budget / surface

| Command | Exit | Summary |
|---|---|---|
| `npm test` | **0** | `unit checks passed; monte-carlo: 300 runs, …` |
| `npm run test:ci` | **0** | ci contract + module boundaries + baseline tools |
| `node tools/verify-production-surface.mjs` | **0** | production surface clean (12 markers) |
| `node tools/check-bundle-budget.mjs` | **0** | entry gzip under max 451584 (pre-Pixi baseline 323218) |

### Focused Playwright (serial, `--workers=1 --no-deps`)

```text
node tools/run-with-strict-e2e-port.mjs -- npx playwright test \
  pixi production-profile input-router --project=desktop --workers=1 --no-deps
```

| Result | Count |
|---|---|
| passed | **23 / 23** |
| exit | **0** |

Also: Task 6 fixture isolation **pass**; desktop recovery + production-profile recheck **pass**.

```text
node tools/run-with-strict-e2e-port.mjs -- npx playwright test production-profile \
  --project=iphone-webkit --project=ipad-webkit --workers=1 --no-deps
```

| Result | Count |
|---|---|
| passed | **4 / 4** |
| exit | **0** |

### Perf (unchanged policy; prior PREFIX EXIT measurements still valid)

| Tier | Exit | Notes |
|---|---|---|
| portrait / LITE | **0** | met ≥55 fps / ≤22 ms |
| desktop / Full | **0** + **PERF_WARNING** | valid target miss, non-blocking |

---

## Non-command evidence (GO tip)

| Area | Evidence |
|---|---|
| Trace parity | Task 6 comparison-only fixtures + separate pixi kind/generation asserts — **pass** |
| Pointer / cancel | Focused `input-router` — **pass** |
| Context recovery | Focused `pixi` recovery + WebKit-safe teardown — **pass** |
| Production-profile | Chromium desktop + Playwright WebKit iphone/ipad fresh+veteran — **pass** |
| Production surface | DEV markers absent from release chunks — **pass** |
| Bundle budget | under gzip ceiling — **pass** |
| Task 24 Step 6 baselines | **Deferred** (ship follow-up; not PREFIX EXIT) |
| Safari / Simulator / hardware | **Explicitly none claimed.** |

---

## Cadence notes

- No `npm run test:round5:standing -- --profile p4`
- No full GitHub CI / required-check wait
- No `test:e2e:visual` / `test:e2e:update` in this gate (baselines deferred)
- Prefix ship (`build` + `dist/**` + PR) **not** executed — Decision is **GO TO P5** (continue plan), not prefix exit
- Prior PREFIX EXIT record lived at docs commit `312bfeb0`; this file supersedes that decision with fix evidence above
