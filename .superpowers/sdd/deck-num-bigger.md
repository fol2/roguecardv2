# Deck HUD count bigger

Owner: deck HUD numbers bigger.

`.deck-count` overlay on `.deck-btn` only — asset footprint unchanged.

| Breakpoint | Before | After |
|---|---|---|
| default | 17px | 22px |
| phone (≤1100) | 14px | 18px |
| landscape (≤480h) | 12px | 16px |

Stroke nudged 0.6→0.7px so the thin outline stays readable at the larger size.
